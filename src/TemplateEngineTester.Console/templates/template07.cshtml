@using WebDSL;
@model dynamic
@if(Model.repo != null) { foreach(dynamic item1 in WebDSLExtensions.GetCollection(Model.repo)) { <text> 
  <b>@item1.name</b>
 </text> }}
@if(Model.repo == null || !Model.repo.HasValues) { <text> 
  No repos :(
</text> }